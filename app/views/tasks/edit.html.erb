<h1>id: <%= @task.id %> のタスク編集ぺージ</h1>

<%= form_with(model: @task) do |f| %>
    <%= f.label :content, "タスク" %>
    <%= f.text_field :content %>
    
    <%= f.submit "投稿" %>
    
<% end %>

<%= link_to "一覧へ戻る", tasks_path %>